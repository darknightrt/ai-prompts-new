超过10年的next.js前端开发从业经验，主导或参与过多个大型复杂项目的架构设计与开发管理,帮我提示词页面复杂度初级中级高级实现对每个提示词页面分类设置,管理员时可对编辑提示词复杂度初级中级高级分类,新建提示词页面分类区域添加复杂度初级中级高级选择框
删除缓存启动命令Get-Process node -ErrorAction SilentlyContinue | Stop-Process -Force; Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue; npm run dev
根据原 script.js 的逻辑，为你的项目补充三个关键的高级交互功能，使其与原生 JS 版本的效果完全一致打字机特效 (Typewriter)：复刻首页 Hero 区域的动态文字切换效果。
全局消息提示 (Global Toast)：复刻原有的 showToast 功能，提供创建、复制、删除操作的反馈。
图片灯箱 (Lightbox)：在详情页点击图片时查看大图。
础设施补全：在 globals.css 中补全图表动画和滚动显现所需的 CSS 类。
封装交互组件：创建 ScrollReveal 组件，完美复刻原生的 IntersectionObserver 逻辑，实现“所见即显现”。
重构首页：将 src/app/page.tsx 扩充为包含 5 个完整板块的落地页，还原视觉设计。
目标是在保持原有提示词页面（src/app/prompts/page.tsx）功能不变的基础上，进行以下增强：
新增 Hero 区域：在导航栏下方、搜索栏上方插入一个全宽的横幅（Banner）区域。
内容定制：该区域包含一个大标题（“提示词指南”）和一个副标题（“发现复制高质量的 AI 提示词...”）。
动态配置：这些文本内容不能写死，必须集成到现有的 SiteConfigContext 中，由管理员在“全局设置”面板中进行修改。
实现方案
数据层扩展 (SiteConfigContext):
在 SiteConfig 接口中新增 promptsPage 字段，用于存储提示词页面的标题和描述。
设置默认值以匹配你的设计图。
管理后台升级 (AdminPanelModal):
新增一个 "提示词页面设置" 的 Tab 或分区。
提供输入框让管理员修改上述标题和描述。
页面 UI 升级 (src/app/prompts/page.tsx):
引入 useSiteConfig。
在页面顶部（导航栏下方）插入一个新的 Hero Section 组件，渲染配置中的文本。
管理界面卡死问题原因
PromptCard.tsx
 中管理模式的 Overlay 使用了 absolute inset-0，但父容器缺少 relative 定位：
className={`group bg-[#111] border rounded-xl flex flex-col h-full overflow-hidden transition-all duration-300 hover:border-zinc-600 hover:shadow-lg hover:shadow-purple-900/10 ${


